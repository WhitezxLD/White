getgenv().Options = {
    Current = "None",
    V1 = "\192", 
    V2 = string.rep("B", 4200000),
    Undo = "Naruto"
}

local Material = loadstring(game:HttpGet("https://raw.githubusercontent.com/WhitezxLD/Dataloss-white/main/whit"))()

local X = Material.Load({
    Title = "AFS - Whitezx Hub",
    Style = 3,
    SizeX = 500,
    SizeY = 350,
    Theme = "Dark",
    ColorOverrides = {
        MainFrame = Color3.fromRGB(0, 0, 255)
    }
})

local Y = X.New({
    Title = "White Hub"
})

Options.UI = Y.Button({
    Text = "Vc não selecionou o tipo de versão...",
    Callback = function()

        if Options.Current == "None" then return Options.UI:SetText("SELECIONA A VERSÃO CARAI!") end

        if Type == "Start Dataloss" then
            game:GetService("ReplicatedStorage").Remote.SetDungeonSetting:FireServer("Theme",  Options[Options.Current])
            TextField:SetText("Started")
        elseif Type == "Undo Dataloss" then
            game:GetService("ReplicatedStorage").Remote.SetDungeonSetting:FireServer("Theme", Options.Undo)
            TextField:SetText("Undone")
        end
    end
})

local D = Y.Dropdown({
    Text = "Dataloss Type",
    Callback = function(Value)
        getgenv().Type = Value
    end,
    Options = {
        "Start Dataloss",
        "Undo Dataloss"
    }
})

local D = Y.Dropdown({
    Text = "Dataloss Version v1 do white",
    Callback = function(Value)
        Options.Current = Value
    end,
    Options = {
        "V1 não recomendado",
        "V2"
    }
})

local AE = Y.Button({
    Text = "https://discord.gg/y7UdVbQRr9",
    Callback = function()
        print("https://discord.gg/y7UdVbQRr9")
    end
})

local AE2 = Y.Button({
    Text = "Discord Server",
    Callback = function()
        setclipboard("https://discord.gg/y7UdVbQRr9")
    end
})

getgenv().TextField = Y.TextField({
    Text = "Status",
    Type = "Default"
})

while task.wait() do
    if Type then
        if Options.Current == "Nenhuma >:( escolhe uma ai vai" then
        Options.UI:SetText("Seleciona a V2")
        else
        Options.UI:SetText(Type)
        end
    end
end
